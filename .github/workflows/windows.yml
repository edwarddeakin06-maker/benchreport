name: Windows installer
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
      - run: $env:BENCHREPORT_SMOKE_TEST='1'; npm run desktop
      - run: npm run dist:win
      - uses: actions/upload-artifact@v4
        with:
          name: BenchReport-Pro-Windows
          path: dist/*.exe
          if-no-files-found: error
